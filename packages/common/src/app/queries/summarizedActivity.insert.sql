--
-- @license BSD-2-Clause
-- @copyright Mat. 2024-present
--




-- ...
INSERT INTO garmin.summarized_activity (
    user_short_id,
    activity_id,
    name,
    description,
    activity_type,
    user_profile_id,
    time_zone_id,
    begin_timestamp,
    event_type_id,
    rule,
    sport_type,
    start_time_gmt,
    start_time_local,
    duration,
    distance,
    elevation_gain,
    elevation_loss,
    avg_speed,
    max_speed,
    avg_hr,
    max_hr,
    avg_power,
    max_power,
    avg_run_cadence,
    max_run_cadence,
    steps,
    calories,
    bmr_calories,
    start_position,
    aerobic_training_effect,
    norm_power,
    avg_vertical_oscillation,
    avg_ground_contact_time,
    avg_stride_length,
    strokes,
    avg_strokes,
    active_lengths,
    avg_swolf,
    avg_fractional_cadence,
    max_fractional_cadence,
    pool_length,
    avg_stroke_distance,
    avg_swim_cadence,
    v_o2_max_value,
    avg_vertical_ratio,
    avg_ground_contact_balance,
    lactate_threshold_bpm,
    elapsed_duration,
    moving_duration,
    anaerobic_training_effect,
    device_id,
    parent_id,
    min_temperature,
    max_temperature,
    min_elevation,
    max_elevation,
    avg_double_cadence,
    max_double_cadence,
    location_name,
    max_vertical_speed,
    manufacturer,
    lap_count,
    end_position,
    water_estimated,
    min_respiration_rate,
    max_respiration_rate,
    avg_respiration_rate,
    training_effect_label,
    activity_training_load,
    aerobic_training_effect_message,
    anaerobic_training_effect_message,
    moderate_intensity_minutes,
    vigorous_intensity_minutes,
    avg_grade_adjusted_speed,
    is_run_power_wind_data_enabled,
    difference_body_battery,
    workout_feel,
    workout_rpe,
    run_power_wind_data_enabled,
    purposeful,
    auto_calc_calories,
    favorite,
    pr,
    elevation_corrected,
    atp_activity,
    parent
)
VALUES (
    $<user_short_id>,
    $<activity_id>,
    $<name>,
    $<description>,
    $<activity_type>,
    $<user_profile_id>,
    $<time_zone_id>,
    $<begin_timestamp>,
    $<event_type_id>,
    $<rule>,
    $<sport_type>,
    $<start_time_gmt>,
    $<start_time_local>,
    $<duration>,
    $<distance>,
    $<elevation_gain>,
    $<elevation_loss>,
    $<avg_speed>,
    $<max_speed>,
    $<avg_hr>,
    $<max_hr>,
    $<avg_power>,
    $<max_power>,
    $<avg_run_cadence>,
    $<max_run_cadence>,
    $<steps>,
    $<calories>,
    $<bmr_calories>,
    $<start_position>,
    $<aerobic_training_effect>,
    $<norm_power>,
    $<avg_vertical_oscillation>,
    $<avg_ground_contact_time>,
    $<avg_stride_length>,
    $<strokes>,
    $<avg_strokes>,
    $<active_lengths>,
    $<avg_swolf>,
    $<avg_fractional_cadence>,
    $<max_fractional_cadence>,
    $<pool_length>,
    $<avg_stroke_distance>,
    $<avg_swim_cadence>,
    $<v_o2_max_value>,
    $<avg_vertical_ratio>,
    $<avg_ground_contact_balance>,
    $<lactate_threshold_bpm>,
    $<elapsed_duration>,
    $<moving_duration>,
    $<anaerobic_training_effect>,
    $<device_id>,
    $<parent_id>,
    $<min_temperature>,
    $<max_temperature>,
    $<min_elevation>,
    $<max_elevation>,
    $<avg_double_cadence>,
    $<max_double_cadence>,
    $<location_name>,
    $<max_vertical_speed>,
    $<manufacturer>,
    $<lap_count>,
    $<end_position>,
    $<water_estimated>,
    $<min_respiration_rate>,
    $<max_respiration_rate>,
    $<avg_respiration_rate>,
    $<training_effect_label>,
    $<activity_training_load>,
    $<aerobic_training_effect_message>,
    $<anaerobic_training_effect_message>,
    $<moderate_intensity_minutes>,
    $<vigorous_intensity_minutes>,
    $<avg_grade_adjusted_speed>,
    $<is_run_power_wind_data_enabled>,
    $<difference_body_battery>,
    $<workout_feel>,
    $<workout_rpe>,
    $<run_power_wind_data_enabled>,
    $<purposeful>,
    $<auto_calc_calories>,
    $<favorite>,
    $<pr>,
    $<elevation_corrected>,
    $<atp_activity>,
    $<parent>
)
RETURNING *;
